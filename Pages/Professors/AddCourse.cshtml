@page
@model AddCourseModel

@{
    ViewData["Title"] = "Add Course";
}

<h1>Create</h1>

<h4>Course</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div class="form-group">
                <label asp-for="Course.ProfessorId" class="control-label"></label>
                <select asp-for="Course.ProfessorId" asp-items="Model.ProfessorsDropDown" class="form-control">
                    <option value="">--Select Professor--</option> 
                </select>
                <span asp-validation-for="Course.ProfessorId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Course.Description" class="control-label"></label>
                <input asp-for="Course.Description" class="form-control" />
                <span asp-validation-for="Course.Description" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Add Course" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}